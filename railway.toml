[build]
builder = "NIXPACKS"
buildCommand = "npm run build:backend && echo '=== BUILD OUTPUT ===' && find dist -name 'server.js' -type f 2>/dev/null && echo '=== END ==='

[deploy]
startCommand = "npx tsx src/backend/server.ts"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
