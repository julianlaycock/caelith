[build]
builder = "NIXPACKS"
buildCommand = "npm run build:backend"

[deploy]
startCommand = "npx tsx scripts/migrate.ts && npx tsx src/backend/server.ts"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
